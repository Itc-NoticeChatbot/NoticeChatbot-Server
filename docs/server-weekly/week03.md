# 3주차 - 스프링 부트 기초

## 핵심 개념
- 프로젝트 구조(`src/main/java`, `src/main/resources`)
- `templates`와 `static`의 역할 분리
- `DemoApplication`과 `@SpringBootApplication`
- JPA/ORM 기초
- 엔티티 기본 어노테이션(`@Entity`, `@Id`, `@GeneratedValue`)

## 실습 포인트
- `application.properties`에서 JPA 설정
- 엔티티 클래스 기본 골격 구성

## 교안에서 확인되는 코드 키워드
- `SpringApplication.run(...)`
- `spring.jpa.hibernate.ddl-auto=update`
- `org.hibernate.dialect.H2Dialect`
